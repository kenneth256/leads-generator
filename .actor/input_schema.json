{
  "title": "Leads Generator Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "platforms": {
      "title": "Platforms",
      "type": "array",
      "description": "Select platforms to generate leads from",
      "editor": "select",
      "items": {
        "type": "string",
        "enum": ["github", "reddit", "devto", "hackernews", "producthunt"]
      },
      "default": ["github", "devto", "hackernews"]
    },
    "keywords": {
      "title": "Keywords",
      "type": "array",
      "description": "Keywords to find your target audience (e.g., 'startup', 'founder', 'SaaS')",
      "editor": "stringList",
      "default": ["startup", "founder", "developer"]
    },
    "maxLeads": {
      "title": "Maximum Leads",
      "type": "integer",
      "description": "Maximum number of leads to collect",
      "default": 500,
      "minimum": 1,
      "maximum": 10000
    },
    "leadType": {
      "title": "Lead Type",
      "type": "string",
      "description": "Type of leads to focus on",
      "enum": ["tech", "business", "general"],
      "default": "tech"
    }
  },
  "required": ["platforms", "keywords"]
}
